# 生产环境配置示例
# 此文件说明如何为不同的部署环境配置前端

# ==================== 部署在上海服务器 ====================
# 当前端和后端在同一服务器时，使用相对路径
# API_BASE=/api

# ==================== 部署在纽约服务器 ====================
# 当前端部署在纽约，后端在上海时，使用完整 URL
# API_BASE=https://autooverview.snappicker.com/api

# 注意：
# 1. 生产环境的 API_BASE 配置已在 src/api.ts 中自动处理
# 2. 代码会根据 hostname 自动判断使用哪个 API 地址
# 3. 无需手动配置环境变量

# ==================== 验证配置 ====================
# 部署后，在浏览器控制台运行以下代码验证：
# console.log(window.location.hostname)
# console.log(window.fetch('/api/health'))
# 应该看到正确的请求地址

# ==================== 构建命令 ====================
# 上海服务器部署：
# npm run build

# 纽约服务器部署：
# npm run build
# 构建产物是相同的，代码会自动适配
